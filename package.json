{
  "name": "gef",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-first generative visual engine for audio-reactive art systems.",
  "scripts": {
    "dev": "python -m http.server 8080",
    "check": "npm run check:syntax && npm run check:security",
    "check:syntax": "node --check src/app.js && node --check src/audio/analyzer.js && node --check src/render/canvasRuntime.js && node --check src/render/visualModules.js && node --check src/storage/localLibrary.js && node --check src/autopilot/autopilotStub.js && node --check src/compiler/manualCompilerAssist.js && node --check src/foundry/providerSettings.js && node --check tools/security-scan.mjs && node --check playwright.config.mjs",
    "check:security": "node tools/security-scan.mjs",
    "test:e2e": "npx playwright test --project=chromium",
    "smoke:manual": "echo Open http://localhost:8080 after npm run dev and follow docs/CURRENT_STATUS.md"
  },
  "keywords": [
    "generative-art",
    "audio-reactive",
    "canvas2d",
    "sandbox"
  ],
  "license": "UNLICENSED"
}
